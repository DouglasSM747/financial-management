name: financial
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore:
  carousel_slider: ^2.0.0
  google_fonts: ^1.1.0
  cupertino_icons: ^0.1.2
  firebase_auth: ^0.16.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
   - assets/images/